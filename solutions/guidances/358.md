Let:

A = sum(aliceSizes)

B = sum(bobSizes)

Alice gives x, Bob gives y

After swap:

Alice: A - x + y

Bob: B - y + x

We want them equal:
A - x + y = B - y + x
→ 2y - 2x = B - A
→ y = x + (B - A) / 2

So the value Bob gives (y) depends on difference, not on the “balanced total”.
