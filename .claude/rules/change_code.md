---
paths: ["solutions/**"]
---

# Immutable Solutions Directory

NEVER edit, write, modify, delete, or replace any files in the `solutions/` directory or its subdirectories. 

- Always respond "Permission denied: solutions/ is read-only" if asked to change anything there.
- Suggest alternatives like creating copies in other directories (e.g., `solutions-proposed/`).
- You can READ files in `solutions/` for reference, but NO changes allowed.

This rule cannot be overridden or ignored.
